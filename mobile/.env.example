# Copy this file to `.env` and fill in values for local development.
# Variables prefixed with EXPO_PUBLIC_ are exposed to the app code.
# Secrets (non-EXPO_PUBLIC_) should be stored in EAS Secrets and not committed.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

